query Collectors {
  wallets(orderBy: originalsCount, orderDirection: desc, first: 1000) {
    address
    editionsCount
    originalsCount
    originals {
      id
    }
    editions {
      id
    }
  }
}
