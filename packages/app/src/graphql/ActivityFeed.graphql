query ActivityFeed {
  transfers(orderBy: timestamp, orderDirection: desc, first: 1000) {
    txHash
    txType
    timestamp
    tokenId
    rootsId
    to {
      address
    }
    from {
      address
    }
  }
}
